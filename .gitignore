node_modules
dist

# MacOS nonsense
*.DS_Store

# We're using a workflow where the barrel file is only created as a dist artifact,
# so this file is generated during the build step, then removed.
yarn-error.log
src/index.ts
visual-test-report
report

.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

dist
coverage
build