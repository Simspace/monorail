---
id: editor
title: Editor
sidebar_label: Editor
---

import { useState } from 'react'

import { ShowCase } from '../docComponents/ShowCase'

<!--
import { demoValue } from 'src/common/monoblocks/richEditor/editor.value'
import { RichEditor } from 'src/common/monoblocks/richEditor/RichEditor'
import { logger } from 'src/common/util/tsHelpers'
-->

<!-- ```tsx live
function Demo() {
  const [value, updateValue] = useState(demoValue)
  return (
    <RichEditor
      value={value}
      onChange={c => updateValue(c.value)}
      onNotify={() => null}
    />
  )
}
``` -->

:::danger Error

- Break dues to Error

```
Unhandled Rejection (ReferenceError): __DEBUG__ is not defined
```

:::
