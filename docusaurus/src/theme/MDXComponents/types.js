import * as ButtonTypes from '@monorail/visualComponents/buttons/buttonTypes'
import { AlertLevel } from '@monorail/visualComponents/toast/types'
import { AlertType } from '@monorail/visualComponents/alerts/alertType'
import { ToggleSize } from '@monorail/visualComponents/toggle/toggleTypes'
import { Step as StepType } from '@monorail/visualComponents/stepper/types'
import { LoaderType } from '@monorail/visualComponents/loading/Loading'

export const MonorailTypes = {
  ...ButtonTypes,
  AlertLevel,
  AlertType,
  ToggleSize,
  StepType,
  LoaderType,
}
