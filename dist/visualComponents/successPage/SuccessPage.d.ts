import React, { FC } from 'react';
declare type SuccessPageProps = {
    titleText?: React.ReactNode;
    subtitleText?: React.ReactNode;
};
export declare const SuccessPage: FC<SuccessPageProps>;
export {};
