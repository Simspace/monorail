import { Meta } from '@storybook/addon-docs'

import { Alert } from '@monorail/components'

<Meta title="Theme/Typography/Guidelines" />

## Guidelines

### Sizing

It is recommended to use our tokenized font sizes at all times. Should you need to assign a custom value, use `theme.typography.pxToRem()`.

**For developers**

<Alert severity="warning">
  There is a known Figma bug that displays the wrong Style name and Typography
  properties. If
</Alert>

If you come across fonts in designs that don’t use Figma’s Typography Styles, consult with your designer/s on what typography token to use.
